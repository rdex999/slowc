#intext to_include.hslw

func global main(argc i32) -> i8
{
	let n i8 = 2;
	let result i8 = (100 / 4) * n;

	// Print "abc\n", just having some fun with the C standard library
	putchar(97);
	putchar(98);
	putchar(99);
	putchar(10);

	return result;
}