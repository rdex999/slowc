#intext to_include.hslw

func global main(argv u64, argc u32) -> u32
{
	let result u32;
	if 4 + 1 * 2 != 5 + 1 * 2 - 2
		result = 1;	
	else
		result = 0;

	return result;

	return 15 - 5 * 3 + 5 * 1 ^ 2 * 1 + 2 | 1 * 2 - 1 * 0;
}